# Base URL for the support-ticket API.
# In dev, leave as /api to use the Vite proxy (see vite.config.js).
# For a deployed backend, set the full origin, e.g. https://api.example.com
VITE_API_BASE_URL=/api
