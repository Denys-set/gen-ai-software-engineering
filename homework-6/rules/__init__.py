"""Configurable rule engine package for the multi-agent banking pipeline.

All tunable policy lives in ``config/rules.json``; :mod:`rules.engine` loads and evaluates it so
the runtime agents read data instead of hard-coding thresholds. See EXTENSION-PROMPTS.md (Task 1).
"""
